/**
 * Id sintético en el JWT cuando el bypass de emergencia no obtuvo fila en Neon.
 * `change-password` debe resolver al usuario real por nombre (contains / startsWith).
 */
export const NUCLEAR_EDUARDO_PLACEHOLDER_USER_ID = "nuclear-eduardo-recovery-fallback";
